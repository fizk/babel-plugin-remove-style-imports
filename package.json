{
  "name": "babel-plugin-remove-style-imports",
  "version": "1.0.0",
  "description": "Removes imports that reference (s)css files from the AST",
  "main": "./lib/index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "install": "npm run build:dist",
    "build:dist": "babel --presets=es2015 src --out-dir lib"
  },
  "keywords": [
    "babel"
  ],
  "author": "Einar Valur",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
